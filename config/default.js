module.exports = {
    kvs: {
        // type = "memory" or "redis"
        type: "memory"

        // type: "redis",
        // host: "127.0.0.1",
        // port: 6379
    },
    session: {
        // type = "memory" or "redis"
        type: "memory"

        // type: "redis",
        // host: "127.0.0.1",
        // port: 6379
    }
};
