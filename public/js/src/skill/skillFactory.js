bq.skill.SkillFactory = cc.Class.extend({
    /**
     *
     * @param {bq.model.Skill} skill
     * @param {bq.model.Entity} user
     * @param targetPos
     */
    create: function(skill, user, targetPos) {

    }
});